'use client';import{useState}from'react';import{loadData}from'../lib/data-store';import{buildLifeList,taxonName}from'../lib/derived';export default function LifeAppStable(){const[d]=useState(loadData());const[l]=useState(()=>buildLifeList(d));return <main><h1>Life</h1><p>{l.length} species recorded</p>{l.map(x=><div key={x.taxon.id}><strong>{taxonName(x.taxon)}</strong><span> {x.observationCount}</span></div>)}</main>}